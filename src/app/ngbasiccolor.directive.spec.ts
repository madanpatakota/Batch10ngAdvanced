import { NgbasiccolorDirective } from './ngbasiccolor.directive';

describe('NgbasiccolorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbasiccolorDirective();
    expect(directive).toBeTruthy();
  });
});
