import { NgadvancedcolorDirective } from './ngadvancedcolor.directive';

describe('NgadvancedcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgadvancedcolorDirective();
    expect(directive).toBeTruthy();
  });
});
